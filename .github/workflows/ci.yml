name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  byte-compile:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        emacs-version: ['27.1', '28.2', '29.4', '30.1', 'snapshot']
    steps:
      - uses: actions/checkout@v4
      - uses: purcell/setup-emacs@master
        with:
          version: ${{ matrix.emacs-version }}
      - name: Byte-compile shared library
        run: |
          emacs --batch -L . \
            --eval "(setq byte-compile-error-on-warn t)" \
            -f batch-byte-compile tokyo-themes.el
      - name: Load all themes
        run: |
          emacs --batch -L . \
            -l tokyo-night-theme.el \
            -l tokyo-storm-theme.el \
            -l tokyo-moon-theme.el \
            -l tokyo-day-theme.el
